# SDD ledger — plan: docs/plans/m4/M4-PLAN.md

> M4 编排 ledger。压缩恢复时信任本文件 + `git log` 胜过自身记忆。
> 分支 `feat/m4-prod-readiness`；无 worktree，主目录串行（同时只一个 implementer 写）。
> 完整计划见 `M4-PLAN.md`。

## 看板（TaskCreate #1–#7）
- Task 1 / Phase 0：测试基础设施设计 + 用户确认 — in_progress
- Task 2 / Phase 1：cargo-fuzz targets — pending
- Task 3 / Phase 2：FaultVfs 崩溃恢复套件 — pending（依赖 Phase 0 FaultVfs 设计）
- Task 4 / Phase 3：跨版本兼容 + 迁移 — pending
- Task 5 / Phase 4：并发压测 + 竞态 — pending
- Task 6 / Phase 5：tracing + inspect + 诊断 — pending
- Task 7 / Phase 6：CI + SPEC v1.5 + 总结 — pending（依赖前序阶段产出）

## 进度
- [turn 1] 建分支 `feat/m4-prod-readiness`；扫 docs/plans 命名约定（沿用 m3：PROGRESS/M4-PLAN/task-N-*）；建看板 #1–#7。
- [turn 2] 落地 `M4-PLAN.md`（计划事实记录）+ 本 ledger；派只读设计 SubAgent（general-purpose / opus / background，agentId `a310ab7bc723b2723`）出 Phase 0 设计 → `docs/plans/m4/phase0-design.md`。
- [turn 3] 设计 SubAgent 首次回报状态：读完全部上下文（26 次工具调用 / ~535s）后停在"开始写文档"前，**`phase0-design.md` 未产出**（ls 确认；git status 确认无源码误改，只读约束守住）。已 SendMessage 恢复同一 agent（上下文完整，省去重读），令其**别再读、直接一次 Write 写出完整六项设计 + 回报契约**。二次回报见 turn 4。
- [turn 4] 二次回报：0 工具调用、~310s，只返回"现在立即开始撰写"意图文本，**`phase0-design.md` 仍未产出**。同一 SubAgent 失败 2 次（达 orchestrator 上限），**换策略**：弃用 a310ab7bc723b2723，派新 Plan agent（opus / background / 只读无 Write 权限——根除 Write-stall / 聚焦读 7 文件非 26 留上下文预算 / 产出方式=最终文本回复完整设计正文，编排者落档 phase0-design.md / 铁律"禁止 announce intent 直接返回正文"）。回报见 turn 5。
- [turn 5] Plan agent 回报 38 工具调用/~395s 但**无 `<result>`**（停在工具调用上、未输出最终文本——opus 重读后不产出大文本的同类问题；文件系统确认无源码误改、`phase0-design.md` 仍未产出，Plan 无 Write 权限符合预期）。已 SendMessage 恢复同一 agent，令其**不调任何工具、直接输出设计正文为最终回复**（廉价恢复省重读）。若仍失败（agent2 第 2 次失败）即判阻塞，AskUserQuestion 上报用户拍板（选项：编排者自拟设计/精简范围/换 sonnet/中止）。
- [turn 6] **恢复成功**：Plan agent 二次执行返回完整设计正文（状态 DONE）——六项逐项设计（FaultVfs path+op+调用计数器 层 1+2，不实现层 3 hook / cargo-fuzz 独立 crate `vane-fuzz` + workspace `default-members` 排除 + CI test job 加 `--exclude vane-fuzz` + nightly / proptest 三不变量 + `proptest-regressions/` 提交 / 跨版本 fixture 提交仓库 `tests/fixtures/compat/v0.1.0/` / tracing feature 默认 off，`tracing` crate 不触黑名单 / inspect API `Db::stats`+`segment_info` 纯新增，Vfs 无 size 方法→read_at 探测 EOF 或 `Option<u64>`）+ 现状摸底（Vfs trait 方法表 sync+Send+Sync+`Arc<dyn Vfs>` / 持久化关键点 file:line / 测试布局 / CI 16 jobs）+ SPEC 影响清单（§9/§10/§13.2/§14）+ 9 未决问题 + 执行摘要。无硬 SPEC 矛盾（4 处属 v1.4→v1.5 演进）。编排者落档 `docs/plans/m4/phase0-design.md`。git tag 查询并行（未决问题 #1 事实问题自查）。下一步 AskUserQuestion 检查点（3 决策：设计+SPEC 批准 / FaultVfs cfg(test) vs +dev-feature / LostWrite 列 Could vs 必实现）。
- [turn 7] **Phase 0 用户确认通过**：Q1 批准设计进实现 / Q2 FaultVfs = cfg(test)+dev-feature（两者） / Q3 LostWrite 列 Could 暂不实现（从 Fault enum 省略、留 TODO 注释，崩溃恢复用 sync 失败注入近似）。Task #1 complete。开始实现——按设计推荐顺序 **FaultVfs 先行**（Task #3 Phase 2 的 a 子任务：FaultVfs 机器+单测；crash_recovery 5 场景为 b 子任务随后；Task #2 fuzz 随后）。BASE = 本轮 `git rev-parse HEAD` 捕获（git log 可恢复）。派 implementer（sonnet / background）实现 §3.1，brief=`docs/plans/m4/phase0-design.md` §3.1，report=`docs/plans/m4/task-faultvfs-report.md`。
- [turn 8] FaultVfs implementer（sonnet）回报 **DONE**：commit `03319ca`；8 新单测 + 321 全测无回归；fmt/clippy/test/wasm32-check/check-no-std-fs 全绿（wasm32 无 FaultVfs 符号泄漏）；LostWrite 按决策省略+TODO。concerns：(1) 给所有 Fault 变体加 `one_shot`+`trigger_on_nth`（设计骨架只在 IoError 示意，扩展为统一机制——待 reviewer 判是否算偏离"按字面采用"）；(2) `call_counts` one_shot 后未清理（test-only，trivial）。生成 review package `docs/plans/m4/task-faultvfs-review-package.md`（985cc06..03319ca）。下一步派 task reviewer（sonnet）。
- [turn 9] task reviewer 回报：**Spec ✅，0 Critical/Important，7 Minor**（M1 call_counts 同 key 共享→trigger_on_nth 误计[边界，映射表不同 path 不触发，2b 注入多同 path 规则前评估] / M2 PartialWrite inner 错误 `?` 传播[MemoryVfs 主力不受影响] / M3 glob_match 全匹配 vs brief 前缀[实现更精确，design 措辞 reconcile] / M4 trig+one_shot=false 语义[brief 忠实] / M5 catch-all 静默消费不适用 op[已注释] / M6 Mutex unwrap 中毒级联[cfg(test) 标准] / M7 测试缺口[2b 间接覆盖]）。implementer 自报偏离判**不算偏离**。4 ⚠️：wasm32 体积(report 仅 cargo check)/vane-ffi feature(2b)/crash_recovery.rs(2b)/CI yml 未改(**正在读 ci.yml 核验 test job --all-features 编 fault.rs + wasm32-check 不应 --all-features**)。Minor 全 defer final review。**全量门禁确认脚本后台跑**（fmt/clippy workspace/test --workspace --all-features/wasm32 core+fault-injection/wasm32 vane-wasm/check-wasm-size/check-no-std-fs）。等结果→全绿 mark Phase 2a 完成、committable、进 2b；红则 fix。
- [turn 10] **Phase 2a（FaultVfs）全量门禁确认全绿**：fmt rc=0 / clippy workspace --all-features rc=0 / test --workspace --all-features rc=0（321+8 无回归，跨 crate 全过）/ wasm32 core+fault-injection check rc=0（fault.rs 在 wasm 带 feature 可编——sleep 已 cfg-gated；**1 warning：sleep_ms(ms) 在 wasm 上 ms 未用 → Minor #8**，非 CI 门禁，defer final review 或后续触动 fault.rs 时 `let _ = ms;` 修）/ wasm32 vane-wasm rc=0 / check-wasm-size rc=0（vane-wasm 349KB / core --export-all 641KB gzip，均 ≤800KB，fault.rs 不在默认 wasm 体积不变 → ⚠️1 解决）/ check-no-std-fs rc=0。⚠️4 已 ci.yml 核验解决。**Phase 2a complete**（commit 03319ca，review clean，8 Minor deferred：M1-M7 + #8 sleep_ms）。⚠️2/⚠️3 → Phase 2b 范围。开始 **Phase 2b**：crash_recovery.rs 5 场景（meta_slot/WAL/merge/ENOSPC/部分写），用 FaultVfs 在持久化关键点注入，brief=phase0-design.md §3.1 注入点映射表 + §2.2 file:line + M4-PLAN 阶段二。BASE=03319ca。派 implementer（sonnet / background）。
- [turn 11] Phase 2b implementer 回报 **DONE**：commit `c7e3cdf`（`test(core): crash_recovery 5 场景`）；5/5 场景过（meta_slot/WAL/merge/ENOSPC/部分写）；6 门禁绿（fmt/clippy/crash_recovery 5/--all-features --tests 无回归/默认 0 测试门控/wasm32）；仅 crash_recovery.rs 入 commit。concerns：(1) **segment/header.rs::decode_header latent panic**——`buf.len()==8` 过 `< 8` 门但 `buf[8]` 访问 ulid_len 致 index-out-of-bounds panic（非 VaneError::Corrupt）；场景 5 PartialWrite 写 8 字节，测试**避开直接 SegmentReader::open**（改用 Vfs 直读+recover 孤儿清理间接验证）；建议 `< 8`→`< 9`（segment 模块，本任务未改）——编排者正 grep 核验。(2) examples/gen_qrels.rs 预存编译错（`main` not found，git stash 验证预存），`cargo test -p vane-core` 不带 --tests 报错；不影响 --tests/--all-features 路径——defer（非 M4 引入，demo 区）。生成 review package（03319ca..c7e3cdf）。下一步派 task reviewer（**opus**——崩溃恢复断言正确性 + vacuous 判 Critical）。
- [turn 12] decode_header bug **grep 核验确认**：`segment/header.rs:39` `if buf.len() < 8` + `:52-53` `let pos=8; let ulid_len=buf[pos]`（`buf[8]`）→ `buf.len()==8` 过门但 `buf[8]` 越界 panic（非 Corrupt），应 `< 9`。预存生产 bug，被场景 5 撞出。diff 范围确认只 crash_recovery.rs +591（无 Cargo.toml/SPEC/CI/fault.rs）。review package 24189B。派 **opus task reviewer**（断言正确性 + 场景 5 间接验证定性 + decode_header 处置建议 + vacuous 判 Critical）。
- [turn 13] opus reviewer 回报：**Spec ✅，0 Critical，2 Important（I-1+I-2 紧耦合），2 Minor（M-1 tmp 清理注释 defer / M-2 注释 mask panic——I-2 修后自愈）**。全 5 场景断言实质性非 vacuous（文档数/external_id contains_id/search 结果/段集合）。场景 5 间接验证判 **Important**（recover orphan 清理不 open 段，decode_header 拒绝路径无场景 exercised）。decode_header 处置：**M4 2b fix 循环修**（panic-on-corrupt-header 与数据安全矛盾，1 行 fix，解锁场景 5 spec 合规）。进 **fix 循环 round 1/5**：复派 sonnet implementer（aebcea64f29b8bd52，上下文在）修 I-2（`<8`→`<9`，授权改 segment/header.rs）+ I-1（场景 5 加 SegmentReader::open/decode_header Corrupt 直接断言）+ M-2 注释校准 + 可选 header.rs 单测；M-1 defer。
- [turn 14] fix r1 implementer 回报 **DONE**：commit `acbd23d`（`fix(segment): decode_header off-by-one + 场景 5 Corrupt 断言`）；I-2 修（`<8`→`<9`）+ I-1 修（场景 5 加 decode_header Corrupt 直接断言）+ 防回归单测 `decode_header_8_bytes_returns_corrupt_not_panic`（1/1 过）+ M-2 注释校准。覆盖测试：crash_recovery 5/5 + header 单测 1/1 + 全套 322 unit+integration 0 FAILED。门禁全绿（fmt/clippy/test crash_recovery/header/--all-features --tests/wasm32 check）。commit 只 segment/header.rs + tests/crash_recovery.rs（2 文件 +34 -5）。concerns：SegmentMeta 无 Debug derive，测试 `{:?}` 触 E0277→移除 `{:?}`（未加 derive 避免碰 M0 冻结 struct）——Minor friction defer。M-1 defer。生成 scoped re-review package（c7e3cdf..acbd23d）。下一步派 re-reviewer（sonnet）验 I-1/I-2 ADDRESSED + fix diff 无新 Critical/Important。
- [turn 15] scoped re-reviewer 回报：**I-2/I-1/M-2 全 ADDRESSED + 无新 Critical/Important → fix 循环完成（无 round 2）**。I-2 `<9` 门正确不误拒（真实 header ≥35 字节）；I-1 场景 5 直接 decode_header Corrupt 断言三重非平凡（Err+Corrupt 变体+"too short" 消息）；header 回归单测有效守护 panic 防回归。M-1 defer。**Phase 2b 定案**（commits 03319ca..acbd23d：crash_recovery 5 场景 + decode_header fix）。fix 触动生产 segment/header.rs（M4 首个生产代码改动）→ 后台跑全量门禁确认（fmt/clippy/test --workspace --all-features/wasm32 core+vane-wasm/check-wasm-size/check-no-std-fs）。等全绿 → **Phase 2（#3）整体完成** → 进 Phase 1（cargo-fuzz + proptest，#2）。recall/nDCG 不重跑（header 改动仅影响 8 字节畸形 header，well-formed ≥35 字节不受影响，workspace test 覆盖 recall.rs/recall_regression.rs/corpus_compat）。
- [turn 16] **全量门禁确认全绿**（fix 触动生产 segment/header.rs）：fmt rc=0 / clippy workspace --all-features rc=0 / **test --workspace --all-features rc=0**（recall/corpus_compat/crash_recovery 全集成测试无回归）/ wasm32 core rc=0 / wasm32 vane-wasm rc=0 / check-wasm-size rc=0（vane-wasm 349261B / core --export-all 641275B gzip，均 ≤800KB）/ check-no-std-fs rc=0。**Phase 2（#3）整体完成**：FaultVfs（2a，03319ca）+ crash_recovery 5 场景（2b，c7e3cdf）+ decode_header 生产 fix（acbd23d）；commits 985cc06..acbd23d；fix 循环 r1 clean，0 Critical/Important。commit docs/plans/m4/ artifacts（Phase 0 设计 + Phase 2 报告/审查）。Task #3 complete。**进 Phase 1**（cargo-fuzz crate + 5 targets / proptest 3 不变量，#2）——按设计推荐顺序（Phase 2 后接 Phase 1）。Task #2 in_progress。下一步派 Phase 1 implementer。
- [turn 17] Phase 2 docs committed（0458942，10 files +3229：M4-PLAN/PROGRESS/phase0-design + task-*-report/review*/review-package*）。分支 985cc06..0458942。**进 Phase 1**（#2 in_progress）。Phase 1 拆 1a（cargo-fuzz crate + 5 targets + workspace members/default-members + CI test job line 55 & clippy job line 44 加 --exclude vane-fuzz + nightly build/smoke + cargo deny 验 libfuzzer-sys）+ 1b（proptest 3 不变量）。先派 **1a cargo-fuzz**（sonnet / bg，BASE=0458942）：brief=phase0-design.md §3.2。编排者并行查 rustup toolchain 知 nightly 可用性。**注意**：1a 后编排者 gate 脚本亦须 `--exclude vane-fuzz`（vane-fuzz 需 nightly，stable test/clippy 编不了）。
- [turn 18] 1a cargo-fuzz implementer **失败**（nightly 安装网络超时 → 600s 无进展被 watchdog 杀）。但**代码已写完**（查证）：crates/vane-fuzz/（Cargo.toml+fuzz_targets/）+ workspace Cargo.toml（members+default-members）+ ci.yml（test line 61 + clippy line 48 --exclude vane-fuzz）+ Cargo.lock。只差验证+commit。**换策略**（失败 1，恢复原 implementer a2d44a412d0ea767e）：nightly 网络不稳会 stall → 改纯 stable 验证 + commit，fuzz build/smoke 延至 CI nightly（Phase 6 fuzz-smoke job）。铁律"禁 rustup install / 禁 cargo +nightly / 禁 cargo install cargo-fuzz"，只 stable：fmt/clippy --workspace --all-features --exclude vane-fuzz/test --exclude vane-fuzz/check -p vane-fuzz（stable 验编译；若需 nightly 则记 concern 延 CI）/deny/wasm check。
- [turn 19] 1a cargo-fuzz implementer 回报 **DONE_WITH_CONCERNS**：commit `d4a94d8`（10 files：crates/vane-fuzz/** + workspace Cargo.toml + Cargo.lock + ci.yml test+clippy --exclude vane-fuzz）。stable 门禁：fmt/clippy --workspace --all-features --exclude vane-fuzz/test --exclude vane-fuzz PASS（322 unit+集成 0 FAILED）/wasm32 core PASS。**cargo check -p vane-fuzz (stable) FAIL（预期）**——libfuzzer 模块在 `cfg(fuzzing)` 后，plain cargo check 不设此 cfg（仅 cargo fuzz 设），非 API 错误非 nightly 问题；5 targets API 签名已 grep 确认。**cargo deny check：bans ok / licenses FAIL on NCSA / advisories+sources ok**——libfuzzer-sys license=(MIT OR Apache-2.0) AND NCSA，deny.toml [licenses] allow 未含 NCSA（阻断 CI deny job）。concerns：(1) **NCSA 阻断**（需 deny.toml 加 NCSA，**AskUserQuestion 用户批准**——license policy，design 未预见）；(2) nightly fuzz build/smoke 未本地验证（网络不稳），延 CI Phase 6 fuzz-smoke job（per-plan）；(3) dict_load_fuzz 未覆盖 JiebaDict::load 畸形字节（不启 jieba feature 避 wasm32 feature unification），defer Phase 6；(4) Cargo.lock 入 commit + clippy --workspace（必要保 CI 绿）。生成 review package（0458942..d4a94d8）。下一步：AskUserQuestion NCSA + 并行派 task reviewer。
- [turn 20] **NCSA 用户批准**（"批准加 NCSA"）。opus reviewer 回报：**Spec ✅，0 Critical（5 targets 全 non-vacuous exercise 不变量），1 Important=NCSA（须 deny.toml 加 NCSA，已批准），3 Minor（M1 persist_roundtrip unwrap_or_default 假绿 / M2 merge_fuzz 用 HNSW search 验 live 致假红/绿，建议 search_brute_baseline / M3 dict_load build_tokenizer 未断言 Err）——全 defer Phase 6 fuzz-smoke 跑通后微调**。nightly 延 CI acceptable；dict_load 不启 jieba acceptable；cfg(fuzzing) stable check fail 是 cargo-fuzz 惯例非问题。crate 本身不进 fix 循环。恢复 1a implementer（a2d44a412d0ea767e）apply NCSA fix：deny.toml [licenses] allow 加 NCSA + cargo deny check 验绿 + commit `chore(deny): allow NCSA`。NCSA fix 后 1a 定案 → 1b proptest。
- [turn 21] NCSA fix DONE：commit `9e262db`（deny.toml +6：`"NCSA"` + 注释，仅 deny.toml）。cargo deny check **licenses FAIL→ok**（advisories+bans+licenses+sources 全 ok，exit 0），[bans] 黑名单语义不变。report §10 追加。**1a cargo-fuzz 定案**：commits 0458942→d4a94d8（crate+5 targets+workspace+CI --exclude）→9e262db（NCSA）；spec ✅ 0 Critical non-vacuous；stable gates 绿（fmt/clippy/test --exclude vane-fuzz/wasm/deny）；nightly fuzz run + 3 Minor（M1/M2/M3）defer Phase 6。编排者跑 cargo deny check 复核。**进 1b proptest**（3 不变量 + NaN 过滤 + cargo deny 验 proptest 依赖链）。
- [turn 22] cargo deny check 复核**全绿**（advisories+bans+licenses+sources ok，exit 0；1 预存 warning[unused-wrapper] regex/napi-derive-backend 非匹配，非 error 非本任务引入）。commit 链确认 0458942→d4a94d8→9e262db。**1a cargo-fuzz 完成**（#2 仍 in_progress，含 1b）。进 **1b proptest**（sonnet / bg，BASE=9e262db）：brief=phase0-design.md §3.3，3 不变量（检索稳定/round-trip/merge 不丢）+ Strategy arb_doc/batch/query + NaN 过滤 + proptest-regressions 提交 + cargo deny 验 proptest 依赖链（若拉 regex 上报）。门禁用新基准 `--workspace --all-features --exclude vane-fuzz`（vane-fuzz 需 nightly）。
- [turn 23] 1b proptest implementer 回报 **DONE_WITH_CONCERNS**：commit `f849c7b`。3 不变量（检索稳定/round-trip/merge 不丢）256 cases 全过；cargo deny check 全绿（proptest 不拉 `regex`，仅 `regex-syntax` v0.8.11 独立解析器零依赖非黑名单）。依赖链 bit-set/bit-vec/bitflags/num-traits/rand/rand_chacha/regex-syntax/rusty-fork/tempfile/unarray。concerns：(1) `persist_roundtrip_consistent` --all-features 256 cases ~110s——每例 2× Db::open + jieba feature 冷加载 ~150ms（SPEC 要求 Db::open 加载词典，非缺陷；CI test job 20min timeout 内 OK）；(2) proptest! 宏不自动加 #[test]（已验证修复，256 cases 实跑非 0）；(3) Doc/SearchQuery 无 Debug derive（M0 冻结）→ Strategy 返 Debug tuple + 测试体构造 API 类型；(4) `#![allow(dead_code)]` 文件级（proptest! 宏闭包 dead_code 分析）；(5) 不变量 3 用 search_brute_baseline 验活文档全集（1a M2 建议）。生成 review package（9e262db..f849c7b）。下一步派 opus task reviewer 验 3 不变量 non-vacuous + #[test] 实跑 + 性能 Minor 定性 + Debug tuple workaround。
- [turn 24] opus reviewer 回报 1b：**Spec ✅，0 Critical，1 Important（I-1），3 Minor（M1 Cargo.toml 注释不准 / M2 #![allow(dead_code)] 文件级脆已复核全调用 / M3 persist_roundtrip ~110s 性能 defer）**。#[test] 实跑确认（lines 208/263/341，256 cases 非 0）；Debug tuple workaround 合理不失真；proptest deny 合规（regex-syntax≠regex）。I-1：不变量 1a 仅有上界无下界/非空 guard → search 返 0 假绿风险（当前非假绿，潜在）。进 **fix 循环 round 1/5**：复派 sonnet implementer（a339fe3c84f54f744，上下文在）修 I-1（加 Vector/Hybrid 非空 guard ~5 行）+ M1（修 Cargo.toml 注释）；M2/M3 defer。
- [turn 25] 1b fix r1 implementer 回报 **DONE**：commit `34a9b11`（2 files：proptest_invariants.rs + Cargo.toml）。I-1 修（不变量 1 加 Vector/Hybrid 非空 guard `if matches!(q.mode, Vector|Hybrid) { prop_assert!(!hits1.is_empty()) }`，Text 模式 0 hits 合法不强制）+ M1 修（Cargo.toml 注释改准：proptest default 拉 regex-syntax 非 regex crate）。覆盖测试：3 不变量 256 cases 全过（新 guard 不破坏——Vector/Hybrid 全返非空）+ workspace 无回归。门禁全绿（fmt/clippy/test proptest 3 passed 0 failed 109.82s/test --workspace --exclude vane-fuzz exit 0/deny all ok）。M2/M3 defer。生成 scoped re-review package（f849c7b..34a9b11）。下一步派 re-reviewer（sonnet）验 I-1/M1 ADDRESSED + fix diff 无新 Critical/Important。
- [turn 26] scoped re-reviewer 回报：**I-1 + M1 全 ADDRESSED + 无新 Critical/Important → 1b fix 循环完成（无 round 2）**。I-1 guard 仅 Vector/Hybrid（Text/Auto 不强制，Auto 策略不生成安全）、逻辑正确（docs 非空+query 非全零+topK≥1，Vector/Hybrid 必返非空）、放置正确（vacuous-prone 检查前）、不破坏 256 cases。M1 注释改准。2 deferred Minor（文件级 string_regex 注释措辞含混 / Auto 不被 guard 覆盖但策略不生成）。**Phase 1b 定案**（commits 9e262db→f849c7b→34a9b11：proptest 3 不变量 + I-1 guard fix）。编排者跑 cargo deny check 复核 + commit Phase 1 docs。**Phase 1（#2）整体完成**：cargo-fuzz crate+5 targets+NCSA + proptest 3 不变量。进 **Phase 3**（跨版本兼容 fixture + 迁移，#4）——按设计推荐顺序（Phase 1 后接 Phase 3）。
- [turn 27] cargo deny check 复核全绿 + Phase 1 docs committed（`1fc03af`，8 files +2801）。分支 985cc06..1fc03af。**Phase 1（#2）完成**（cargo-fuzz crate+5 targets+NCSA + proptest 3 不变量+I-1 guard）。#2 completed、#4 in_progress。进 **Phase 3 a**（跨版本兼容，sonnet / bg，BASE=1fc03af）：brief=phase0-design.md §3.4，cross-tag build（git worktree v0.1.0）生成真实 v1 fixture + tests/cross_version_compat.rs（reads_v0_1_0_fixture + v1_v2_coexist + migrates 占位）+ scripts/gen_compat_fixture.rs + format-freeze-note.md（供 Phase 6 SPEC §6.2）。v0.1.0 tag 已确认在。Phase 3 最复杂（cross-tag build），sonnet 若 2 失败换 opus。
- [turn 28] Phase 3a implementer 回报 **DONE_WITH_CONCERNS**：commit `6300392`（fixture 9 文件 36KB + cross_version_compat.rs 446 行 3 测试 + README + gen_compat_fixture.rs + format-freeze-note.md）。全门禁绿（fmt/clippy/cross_version_compat 2 passed 1 ignored/workspace 0 FAILED 322+集成/deny ok/wasm32 rc=0）。worktree 清理无残留（git worktree list 仅主）。concerns：(1) **v0.1.0 vectors.bin 实为 V2 非 V1**（设计 §3.4 "v1 fixture" 措辞不精确——v0.1.0 segment/mod.rs 与当前 diff 空，v0.1.0 始终写 V2 vectors 含 dim 头，无已发布 V1 vectors 产物；VECTORS_FORMAT_V1 读路径无 released fixture 覆盖；"v1/v2 共存"在 stored.bin 维度实现——fixture stored V1 + 新 flush V2（zstd-encode 时）；fixture 是真实 v0.1.0 产物，测当前双模读，格式事实非缺陷）；(2) v1_v2_coexist 不启 zstd-encode 退化为双段共存（CI --all-features 含 zstd-encode 真共存被测；non-vacuous 7 docs 全可见 + d0 排首 score=1.0）；(3) report commit hash 微偏（记 c07600e pre-amend，实 6300392——git log 核实）。v0.1.0 API 与当前 diff 空（仅 header.rs/fault.rs/vector 注释差异，fixture 不受影响）。生成 review package（1fc03af..6300392）。下一步派 task reviewer 验 fixture 真实性 + read 测试 non-vacuous + V2-vectors finding 定性 + format-freeze note。
- [turn 29] opus reviewer 回报 3a：**Spec ✅，0 Critical，2 Important（均 plan-doc 记录问题非代码缺陷），3 Minor**。I-1 report hash 偏（c07600e→6300392，编排者已 git log 核实；纯文档记录，report 注"after amend"，git log 为源→accepted）；I-2 spec §3.4 vectors.bin 标 V1 实为 V2（v0.1.0 segment/mod.rs:205 始终写 V2，reviewer git show v0.1.0 + xxd 验 fixture=V2+dim4 核实）——非实现缺陷，implementer 透明纠正（README/format-freeze-note/report 记录）。Minor：fixture 额外 db/ 层级（功能正确，README 文档化）/ 7 段文件含 hnsw.bin（HNSW_FORMAT_V1 真实常量）/ 无独立 cross-version CI job（经现有 test job，§3.4 允许）。fixture 真实性多源验证（v0.1.0 产物）；跨版本有效性：有价值（真实 v0.1.0 数据 + 读路径回归守卫 + header.rs <9 fix 在 fixture 验证）但判别力因格式未分歧受限（v0.1.0→v0.2.0 固有非缺陷）；V2-vectors acceptable（V1 读路径有单测 segment/tests.rs:421/859）；v1/v2 共存 + reads 测试全 non-vacuous；format-freeze note 充分供 Phase 6 §6.2。**不进 fix 循环**（代码 0 Critical/Important；2 Important 是 plan-doc 记录，编排者据 deliverables 已 resolve）。**Phase 3a 定案**（commit 6300392）。3a 是 test+fixture+docs（无生产代码），implementer 门禁全绿 + reviewer 验证充分→编排者信任不复跑。commit Phase 3 docs。**Phase 3（#4）完成** → 进 Phase 5（tracing + inspect + 诊断，#6，首个触动生产代码的 phase——须全量门禁）。
- [turn 30] Phase 3 docs committed（`0cb50e5`，2 files +1002：task-cross-version-review-package.md + PROGRESS）。**Phase 3（#4）完成**。分支 985cc06..0cb50e5。进 **Phase 5 a**（tracing feature，sonnet / bg，BASE=0cb50e5）：brief=phase0-design.md §3.5，cfg(feature="tracing") 零开销埋点（默认 off，I-5）+ tracing optional dep + 埋点位置（search/flush/merge/cache/WAL/dict）+ **wasm 体积不变验证**（tracing off 体积不变 + 编译期消除 grep=0 + vane-wasm 不启 tracing 守 800KB）+ cargo deny 验 tracing 依赖链。**Phase 5 首个触动生产代码**（vane-core 源码埋点）→ 须全量门禁确认。#6 in_progress。
- [turn 31] Phase 5a tracing implementer 回报 **DONE_WITH_CONCERNS**：commit `dae29c6`（5 code files：Cargo.toml/Cargo.lock/collection.rs/page_cache.rs/wal/mod.rs；report 未暂存）。tracing feature cfg 门控默认 off + 9 埋点（search span/elapsed, flush done, merge start/done, PageCache hit/miss, Wal::append, 词典状态机 3）+ 选"直接各模块 `#[cfg(feature="tracing")] tracing::info!`"不建 telemetry.rs。**wasm off 不变**（vane-wasm 349261B / core --export-all 641277B，与 Phase 2b 同；grep -c tracing = 0 编译期消除确认）+ tracing on wasm 656422B（+15KB ≤800KB）。cargo deny 绿（tracing 链 pin-project-lite/tracing-attributes/tracing-core/once_cell 无黑名单）。全门禁绿（fmt/clippy/test --workspace --exclude vane-fuzz EXIT=0 322+集成/deny/wasm32 off+on check/check-wasm-size/grep=0）。concerns（5 全 defer Phase 6 非阻塞）：(1) CI 无 wasm32-size-tracing-on job（Phase 6）；(2) check-wasm-size.sh 无 grep 断言（Phase 6 持久化）；(3) tracing-subscriber 不进 core（设计取舍）；(4) **wasm elapsed 用 web_time::Instant**（performance.now() ms 精度，known-limitation——**新增 web_time dep 未在设计 §3.5，reviewer 须验 dep 设置 optional/gated + 不影响 wasm off 体积**）；(5) info_span! RAII guard 错误路径不 emit elapsed（defer）。生成 review package（0cb50e5..dae29c6）。下一步派 task reviewer 验 埋点 non-vacuous + cfg 门控 + wasm off 不变 + web_time dep 定性 + deny。
- [turn 32] opus reviewer 回报 5a：**Spec ✅，0 Critical，0 Important，3 Minor**。cfg 全门控（11 宏调用 + let 绑定同 gate + dep:tracing 隔离无泄漏）；埋点 non-vacuous（9 处 emit 有意义字段，flush done 缺 bytes Minor #1 Vfs 无 size() defer inspect/Phase 6）；wasm off 不变确认（grep=0 + 349261B 持平 Phase 2b）；**web-time 定性：`web-time="1"` Cargo.toml:33 连字符 regular dep，PRE-EXISTING（persistence AutoCommitter + segment/ulid.rs 早已用，tracing 复用同 Instant 源，非 tracing 引入增量）——编排者 grep 下划线未匹配是连字符故，无 issue**；deny ok。RAII guard 错误路径 Minor defer。3 Minor（flush bytes / report debug! 笔误 / review hash 偏）。**不进 fix 循环**。**Phase 5a 定案**（commit dae29c6）——cfg-gated 无生产行为变更 off，implementer 全量门禁绿 + reviewer 验证充分→信任，全量门禁留 Phase 5 完成时跑。commit Phase 5a docs。进 **5b inspect API**（sonnet / bg）：brief=§3.6，新 pub API Db::stats/segment_info + structs（全 Debug derive 避 SegmentMeta E0277 教训）+ 健康检查 + Vfs 无 size→read_at 探测或 Option + 单测。
- [turn 33] 5b inspect API implementer 回报 **DONE**：commit `684a112`（5 files：api/inspect.rs 新 + db.rs 新方法 + mod.rs mod 声明 + collection.rs snapshot/hnsw_readers/dict_state private→pub(crate) + ?）。7 structs/enums 全 Debug+Clone（Health/ExecutorKind +Copy+PartialEq+Eq）。3 新 pub 方法（stats/segment_info/collection_segment_info）+ dict_available_internal helper。健康检查：SegmentReader::open 失败→Corrupt / hnsw_readers None→Degraded / Jieba+dict 不可用→Degraded / collection=worst。index_bytes：read_at 探测 EOF（8KB 循环，Vfs trait 不改）+ SegmentFileSizes.hnsw=Option<u64>。门禁全绿（fmt/clippy/20 inspect 单测/workspace 0 failed/deny/wasm32）。concerns：无（inspect 重新 open 段做健康检查——遵循 §3.6 表 spec open 失败→Corrupt，性能可接受，自审记录）。生成 review package（3758620..684a112）。下一步派 task reviewer 验新 pub API + structs Debug + 健康检查逻辑 + Vfs size 方案 + pub(crate) 可见性变更 acceptable + 不改冻结 pub API + 测试 non-vacuous。
- [turn 34] opus reviewer 回报 5b：**Spec ✅，0 Critical，0 Important，7 Minor**。新 pub API 正确；pub(crate) widening acceptable（CollectionInner 是 pub(crate) struct，字段 private→pub(crate) 仅 crate 内部，非 pub API surface）；不改冻结 pub API 确认（3 新 pub 方法+7 新 structs 纯新增，db.rs/mod.rs 仅新增，DbInner/Vfs trait/SPEC/CI 未碰）；健康检查逻辑正确（Corrupt/Degraded/Healthy，worst 函数 test 18 验证）；Vfs size read_at 方案正确；20 单测全 non-vacuous；inspect 重新 open 段 acceptable（遵循 normative spec 表）。7 Minor（M1 segment_info 不排序致 FFI JSON 顺序非确定 / M2 段文件部分缺失→Degraded 未实现 / M3 无 Corrupt/Degraded 回归测 / M4 hnsw sz==0→None 混淆 / M5 持 5 read lock 阻塞写 / M6 dict_available 逻辑重复 / M7 jieba cfg 块冗余）——全 defer Phase 6/follow-up（M1+M3 建议小补丁）。**不进 fix 循环**。**Phase 5b 定案**（commit 684a112）。commit 5b docs。进 **5c VaneError 诊断**（sonnet / bg）：brief=§10，丰富 VaneError String payload（段 ULID/docid/操作/建议），不改错误码 -1..-11、不改 enum 签名（避免冻结 API）。
- [turn 35] 5c VaneError diagnostics implementer 回报 **DONE**：commit `5fc4ac4`。丰富 ~25 VaneError 构造点（open/flush/merge/search/reindex/dict/manifest/WAL/DB open）String payload（段 ULID/docid/操作/建议），**错误码 -1..-11 不变 + VaneError enum 签名不改**。新增 `types.rs append_context(e,ctx)` pub(crate) helper（String 变体追加，无 String 变体原样返，code() 不变）+ `segment/mod.rs segment_ulid_from_dir/seg_ctx` 段级 helper。10 关键路径文件丰富。**crash_recovery 5 场景仍绿**（丰富仅在 CONSTRUCTION 点非 `?` 传播点，不破 contains("manifest.json.tmp")/("inverted.bin")/("ENOSPC")/("partial write")/("too short") 断言）。4 新测试（append_context + open/manifest/wal context）non-vacuous（检 seg=/op=/建议 关键词）。门禁全绿（fmt/clippy/test 346+集成 0 failed/deny/wasm32）。concerns：无阻塞（defer 低优先 leaf 错误 decode_kv_map/stored/scalars ~30 + InvertedIndexReader ~15 + hmm.rs 5，无段级上下文或上层 wrap 间接覆盖，ROI 低 defer；结构化上下文 Could defer Phase 6）。未碰 SPEC/CI/fault.rs/crash_recovery/vane-fuzz/proptest/cross_version/tracing/inspect。生成 review package（8959337..5fc4ac4）。下一步派 task reviewer 验无错误码变更 + 无 enum 签名变更 + String 丰富有意义 + 4 测试 non-vacuous + crash_recovery 仍绿 + 覆盖度。
- [turn 36] opus reviewer 回报 5c：**Spec ✅，0 Critical，0 Important，4 Minor**。**无错误码变更 CONFIRMED**（git diff types.rs grep code/enum/=> -N 空；code() -1..-11 与 SPEC §10 对齐；error_code_matches_spec 测试未改）；**无 enum 签名变更 CONFIRMED**（11 变体字段未改，仅新增 pub(crate) append_context/segment_ulid_from_dir/seg_ctx）；String 丰富 **non-vacuous ADDITIVE**（25+ 构造点含 seg/db/path+op+建议，原消息保留为前缀，无占位）；4 测试 non-vacuous（≥3 关键词+原消息保留）；**crash_recovery 仍绿 CONFIRMED**（5 contains 断言全安全——丰富在 CONSTRUCTION 点非 `?` 传播，`too short` 直调 decode_header 不经 append_context wrap，crash_recovery.rs 未改，ADDITIVE 保 contains 匹配）；覆盖度 adequate（10 关键路径全覆盖，~60 leaf 错误 defer 低 ROI）。4 Minor（report in code commit / report §8.1 略 overstated / dict suggestion 泛化 / segment_ulid_from_dir fallback "unknown"）全 defer。**不进 fix 循环**。**Phase 5 完成**（5a tracing + 5b inspect + 5c 诊断，commits dae29c6/684a112/5fc4ac4，全 0 Critical/Important）。后台跑**全量门禁确认**（Phase 5 生产里程碑：fmt/clippy/test --workspace --exclude vane-fuzz/wasm core+vane-wasm/check-wasm-size/check-no-std-fs/deny + wasm core --features tracing on——tracing off 体积不变 + on ≤800KB 确认）。等全绿 → commit Phase 5 docs + #6 完成 → Phase 4 压测（#5）。
- [turn 37] **全量门禁确认全绿**（Phase 5 生产里程碑）：fmt rc=0 / clippy workspace --all-features --exclude vane-fuzz rc=0 / **test --workspace --all-features --exclude vane-fuzz rc=0**（cumulative 5a+5b+5c + 全前序无回归）/ wasm32 core (tracing off) rc=0 / wasm32 vane-wasm rc=0 / **check-wasm-size rc=0**（vane-wasm 351731B / core --export-all 649096B gzip，均 ≤800KB——**体积较 Phase 2b 增 +2470B/+7821B，源于 inspect 新 pub API + 诊断 String 字面量（非 tracing：tracing off grep=0 无符号），800KB 红线守住**）/ check-no-std-fs rc=0 / deny rc=0 / wasm32 core --features tracing (on) rc=0。**Phase 5（#6）完成**：5a tracing（cfg 门控零开销，off 无符号）+ 5b inspect（新 pub API，不改冻结）+ 5c 诊断（错误码+enum 不变，ADDITIVE String，crash_recovery 仍绿），commits dae29c6/684a112/5fc4ac4，全 0 Critical/Important。commit Phase 5 docs。#6 completed、#5 in_progress。进 **Phase 4 并发压测**（多线程 search+insert+flush+merge N 轮 + Send/Sync 边界 + 可选 loom）——按设计推荐顺序（Phase 5 后接 Phase 4）。
- [turn 38] Phase 5c docs committed（`d6daf7b`，3 files +1303）。#6 completed、#5 in_progress。分支 985cc06..d6daf7b。进 **Phase 4 并发压测**（sonnet / bg，BASE=d6daf7b）：brief=M4-PLAN 阶段四 + 设计阶段依赖（Phase 4=真实并发非 FaultVfs）。多线程 search+insert+flush+merge N 轮（MemoryVfs+tempdir）验无 panic/死锁/数据不一致 + Send/Sync 边界静态断言+跨线程共享 + 纯 stress 竞态检测（**不用 loom**——loom 须 loom::sync 改造 vane-core 侵入大，vane-core 用 std::sync；loom 列 Could defer）+ 段/Meta 锁竞争场景。多次跑确认无 flaky。Phase 4 是 TEST 任务（不改生产代码）。
- [turn 39] Phase 4 stress implementer 回报 **DONE_WITH_CONCERNS**：commit `354f66e`（tests/stress_concurrency.rs 782 行 8 测试）。8 压测过（4-8 线程×50-100 轮）+ Send/Sync 编译期断言 + 3 次全跑+5 次 multi-run 无 flaky。门禁全绿。**但撞出 2 个真实生产并发 bug**（TEST 任务未改生产代码，workaround 规避）：(1) **并发 flush manifest 损坏**——`ManifestStore::save_atomic` 用固定路径 `manifest.json.tmp`，并发调用互相覆盖→E_CORRUPT；测试用 `flush_lock: Mutex<()>` 序列化 flush 规避。(2) **auto-merge 段状态竞争**——`auto_merge_two_smallest` 不检查 `compacting` 锁，与并发 auto-merge/compact 的 `merge_segments` 竞争→段未正确移除→double-count（同文档旧段+merged 段两次）；flush 串行下仍偶发（multi_run 3 次 0-2 失败）；缓解 multi_run_stability 用 flush_interval=25（段数<SEGMENT_MAX=10 不触发 auto-merge）→稳定。**这是 M4 生产门槛核心发现**（数据损坏 bug）——编排者正 grep 核实 save_atomic/auto_merge_two_smallest，确认后将 AskUserQuestion 交用户拍板（fix now 生产修复 vs defer 文档+ship）。
- [turn 40] **2 并发 bug grep 核实确认**：Bug 1 `save_atomic`（persistence/mod.rs:104-121）用 `self.tmp_path()` 固定 manifest.json.tmp，并发 delete/create/write/sync/rename 交错→损坏（确认）。Bug 2 `auto_merge_two_smallest`（collection.rs:486-508）读 snapshot→pick candidates→merge_segments，**不获取 compacting 锁**（compact 在 :1226/:1137 获取+E_BUSY，但 auto_merge 绕过）→与并发 compact/merge 竞争→double-count（确认）。两 bug 真实生产数据损坏。**AskUserQuestion 交用户拍板**：fix both now（Bug 1 唯一 tmp 路径 / Bug 2 auto_merge 获取 compacting 锁）/ fix Bug 1 only / defer 文档+ship。
- [turn 41] **用户批准 fix both now**。派并发 bug fix implementer（sonnet / bg，BASE=354f66e）：Bug 1 `save_atomic`（persistence/mod.rs:104-121）并发 manifest 损坏——方案 A（推荐）ManifestStore 加 flush_lock 序列化 save_atomic，或方案 B 唯一 tmp 路径+glob 清理；Bug 2 `auto_merge_two_smallest`（collection.rs:486-508）不获取 compacting 锁——复用 compact 的 panic-safe Drop guard 模式（try_lock，held→skip 安全降级，空闲→set true+guard→pick+merge）。更新 stress 测试去 workaround（flush_lock + auto-merge 规避）+ 加并发 flush 不损坏 + 并发 auto-merge 不 double-count 测试 + 多次 multi-run（5-10）验无 flaky。不改冻结 pub API（ManifestStore 加 private 字段或方法内锁；auto_merge 是 private fn）。门禁含 crash_recovery（save_atomic 改不破崩溃恢复）。fix 后 opus reviewer 验并发正确性（死锁/lock 序）。
- [turn 42] 并发 bug fix 回报 **DONE_WITH_CONCERNS**：commit `cedbb17`。**实际修复比 task 更深**——Bug 2 根因（task "auto-merge 不获取 compacting 锁"）不完整；stress 实测 compacting guard 单独不够（8x 全 FAILED double-count 18/missing 85）。完整 fix 4 层：(1) compacting guard（task 要求）；(2) `merge_segments target_docid_base` 并入 `next_docid`+原子 read+bump 预留 docid 区间（消 flush 缓冲段与 merge 新段 docid 重叠）；(3) flush `base_docid` 连续性检测（连续→首文档 docid 保 inspect base=0；非连续→rebase next_docid）；(4) merge 快照重建不再用 stale `offsets` 覆写 `seg_offsets`（并发 flush 新段 offset 不被错置 0）。Bug 1 用方案 A（save_lock），但 ManifestStore 原 per-flush/merge 新构造致 save_lock 不共享→改 `DbInner.manifest_store` 为 `Arc<ManifestStore>`+`CollectionInner` 持同 Arc 让 save_lock 真共享。门禁全绿（fmt/clippy/stress 8x 10 测试 OK/crash_recovery 5/5/workspace/deny/wasm32）。无冻结 pub API（ManifestStore save_lock private+pub(crate) update；DbInner/CollectionInner pub(crate) 字段 type 改）。concerns：(1) full merge(compact)+并发 flush docid 重叠**部分修**（compact target_base=0+bump next_docid=estimate，但 compact 前已 buffer 的 stale docid 仍可能重叠；`stress_concurrent_add_during_compact` 过但深层 fix defer Could）——**reviewer 须判是否真实残留 race**；(2) auto-merge skip 频率（高并发 try_lock+skip 段数临时累积 10-22 最终收敛，非正确性，Could）。生成 review package（354f66e..cedbb17）。下一步派 **opus reviewer 严验并发正确性**（死锁/lock 序/4 层 fix 声/Arc 共享/pub(crate)/crash_recovery 仍绿/compact+flush 残留 race 定性）。
- [turn 43] opus reviewer 回报 Phase 4 fix：**Spec ✅，0 Critical，0 Important，3 Minor**。Bug 1 save_lock Arc 共享真生效（入口获取+Drop释放，Arc<ManifestStore> DbInner→CollectionInner 真共享，无残留 new，save_atomic_locked 拆出避重入死锁，I16 tmp 清理保留，write_state 在 save_lock 前 drop 无死锁）；Bug 2 4 层全 ADDRESSED（compacting guard try_lock+skip+panic-safe Drop / docid 原子预留 write_state lock 内 read+bump 消 TOCTOU / flush base_docid 连续性 / merge offsets 不覆写）；**残留 race（concern #1）adequately mitigated Minor defer——关键判断：数学推导排除 compact+并发 flush docid 重叠（compact 新段 [0,actual_new_count) ≤ estimated ≤ next_docid，buffered docs ≥ old_next_docid ≥ actual_new_count，不重叠；compact is_full_merge 不 bump next_docid）**；auto-merge skip acceptable；stress non-vacuous+8x 无 flaky；不改冻结 pub API（pub(crate)）；crash_recovery 仍绿（save_atomic_locked 同 tmp+同 Vfs 序+同错误传播，crash_1/crash_3 断言匹配）；reindex 改动正确。3 Minor（缺 compact+并发 flush 直测[数学排除但无直测] / StdFsVfs append 非原子 pre-existing / merge WAL append lock 不一致非正确性）全 defer。**不进 fix 循环**。后台跑**全量门禁确认**（Phase 4 生产并发 fix 最高风险：fmt/clippy/test --workspace --exclude vane-fuzz/wasm/deny + **stress 2x 额外 multi-run 验无 flaky/race**，共 3x stress）。等全绿 → Phase 4 完成 → Phase 6 CI+SPEC。
- [turn 44] **全量门禁 + 3x stress 全绿**（Phase 4 并发 fix 确认）：fmt rc=0 / clippy rc=0 / **test --workspace --all-features --exclude vane-fuzz rc=0**（含 stress run 1 + crash_recovery + 全集成无回归）/ **stress run 2 rc=0**（10 passed 0 failed 3.83s）/ **stress run 3 rc=0**（10 passed 0 failed 2.81s）——3x stress 无 flaky/race，并发 fix 确认 / wasm32 core rc=0 / **check-wasm-size rc=0**（vane-wasm 352011B / core --export-all 649619B gzip ≤800KB，较 Phase 5 微增 +280B/+523B 源于并发 fix 新代码 save_lock/compacting guard/docid 预留）/ check-no-std-fs rc=0 / deny rc=0。**Phase 4（#5）完成**：stress 8 压测 + Send/Sync + 2 并发 bug fix（Bug 1 save_lock Arc + Bug 2 4 层），3x stress 无 flaky 确认。commits 354f66e（stress）+cedbb17（fix）。commit Phase 4 docs。#5 completed、#7 in_progress。进 **Phase 6 最终阶段**（CI 新 job fuzz-smoke/fuzz-long/compat/stress/crash-recovery + SPEC v1.5 修订 §9/§10/§13.2/§14 **用户批准** + tracing 默认 off + M4 总结报告）。
- [turn 45] Phase 4 docs committed（`f793e93`，4 files +2064）。#5 completed、#7 in_progress。分支 985cc06..f793e93。进 **Phase 6 a CI 新 job**（sonnet / bg，BASE=f793e93）：brief=M4-PLAN 阶段六 + 设计 §3.2 fuzz 草案。加 5 CI job：fuzz-smoke（nightly+cargo-fuzz+60s/target push/PR）/ fuzz-long（cron+workflow_dispatch 10min/target）/ compat（cross_version_compat，合并 corpus-compat 或独立）/ stress（stress_concurrency --release+multi-run）/ crash-recovery（crash_recovery --release）。compat/stress/crash-recovery 评估与现有 test job 冗余（test job --all-features --exclude vane-fuzz 已覆盖 cross_version+stress+crash_recovery debug；独立 job 加 --release/multi-run/可见性）。yml 语法验证。只改 ci.yml。Phase 6 计划：6a CI → 6b SPEC v1.5 修订（§9/§10/§13.2/§14 **用户批准**）→ 6c M4 总结报告 + tracing 默认 off 确认。
- [turn 46] 6a CI implementer 回报 **DONE_WITH_CONCERNS**：commit `b4aa743`（ci.yml +124 仅 ci.yml）。5 新 job：fuzz-smoke（nightly-2026-07-01+cargo-fuzz+5 targets×60s, if:!=schedule, 15min）/ fuzz-long（cron 0 3 * * 0 + workflow_dispatch, if:schedule||dispatch, 60min, 5×600s ||true+artifact）/ compat（needs:test, cross_version_compat --all-features --release, 15min）/ stress（needs:test, stress_concurrency --release ×3, 20min）/ crash-recovery（needs:test, crash_recovery --features fault-injection --release, 15min）。选独立 compat job（DoD 1:1+独立故障信号，非合并 corpus-compat）。YAML 语法验证（pyyaml+yamllint clean）。concerns：(1) **cron 触发整个 workflow**——顶层 on: 加 schedule 致每周日 ALL 21 job 跑（非仅 fuzz-long）；单文件约束 tradeoff（brief 限只动 ci.yml）；public repo CI 免费+兼作 weekly flaky 检测；可后续拆 fuzz-long.yml。(2) nightly-2026-07-01 pin 未本地验证（无法装 nightly/cargo-fuzz）；首次 CI 跑验证。(3) crash artifact 路径未运行时验证（if-no-files-found:ignore 安全）。(4) cargo-fuzz 未 version-lock。生成 review package（f793e93..b4aa743）。下一步派 task reviewer 验 CI job 设计 + cron-tradeoff 定性 + 冗余 + yml 语法 + 不破现有 job + pin。
- [turn 47] 6a CI reviewer 回报：**Spec ✅，0 Critical，0 Important，8 Minor**。5 job sound + 不破现有 job（push/PR 不变，+124 纯追加，needs 链不变，fuzz if: 隔离正确）+ 冗余 acceptable（--release/multi-run/独立信号真实增量）+ yml clean（pyyaml+yamllint）+ fuzz job 设计 sound（artifact if:always+if-no-files-found:ignore 修正 §3.2 矛盾）+ nightly pin acceptable defer。8 Minor（cron 触发全 21 job 周度 flaky 噪音 / concurrency group schedule+push 共享可能 cancel / nightly pin 未本地验证 / fuzz-long ||true 吞编译错 / fuzz-long timeout 60min 紧 / cargo-fuzz 未 version-lock / artifact 路径未运行时验证 / if: 表达式风格不一致）全 defer（post-merge 观察 + tune）。cron-tradeoff acceptable Minor（public CI 免费+flaky 检测+可逆）。**不进 fix 循环**。**Phase 6a 定案**（commit b4aa743，ci.yml 仅）。6a 是 CI yml（不影响 Rust gates）→ yml 语法+reviewer 验证即确认，无 gate run。commit 6a docs。进 **6b SPEC v1.5 修订草案**（sonnet / bg）：brief=phase0-design §5 + M4-PLAN 阶段六 2，起草 §9 inspect/§10 诊断/§13.2 门禁/§14 tracing 到 spec-v1.5-draft.md（暂不改 SPEC.md，待用户批准后 6b-apply）。
- [turn 48] **6b 重派 + 版本号校正**：前次 6b 草案子代理因 API 402 Credit 耗尽终止（外部阻塞，非任务问题）。Credit 恢复，重派新 sonnet implementer（bg）。**编排者起草前 grep 核验撞出两点事实校正，已并入 brief**：(1) **SPEC 当前已是 v1.5**（M3 commit 5d092f8，2026-08-11，changelog 明确"不触碰 §9/§10/§14"）——phase0-design §5 标题"v1.4→v1.5"过时，M4 修订实为 **v1.5→v1.6**，草案文件改 `spec-v1.6-draft.md`（非 spec-v1.5-draft.md）。(2) **FFI 层 inspect 未实现**——Phase 5b 只做 core 层 Db::stats/segment_info（commit 684a112，inspect.rs 7 structs+3 方法），crates/vane-ffi/src/ 无 inspect 函数（grep 确认空）；§9 修订须标 FFI/Node/Wasm 绑定"顺延"（用 v1.5 §12.2"（未实现，顺延）"同款）。两校正均事实明确无歧义，折入草案 + 批准检查点上报用户（不单独中断）。brief 含四节（§9 inspect 顺延 / §10 诊断 ADDITIVE 注 / §13.2 +6 门禁 / §14 I-5 tracing 扩展）+ changelog v1.6 条目 + 待批准检查点 6 决策。Task #7 subject 更正 v1.5→v1.6。
- [turn 49] **草案审查 + 用户批准检查点 + 6b-impl-1 FFI inspect 定案**：Read spec-v1.6-draft.md 全文（287 行）审查——版本号校正正确、FFI 顺延用 §12.2 同款、7 structs 字段从实际读取、偏差透明。核实 3 Minor（§3.6 悬挂引用 inspect.rs 3 处但 SPEC §3 无 3.6→6b-apply 修；proptest commit 草案写 f793e93 实为 f849c7b+34a9b11→apply 修；-Z sanitizer 与 ci.yml 一致）。AskUserQuestion 4 问：Q1 §9 FFI → **用户选"立即实现 FFI 层"**（非顺延，6b 扩范围）；Q2 §10 诊断 → **用户授权"选正确方式，不考虑兼容性/API冻结，项目还没给人用，要求架构精简"**（去 ADDITIVE 妥协，可改 enum 签名，错误码不变）；Q3 §13.2+§13.3+§14 → 批准全部；Q4 版本号 → v1.6 一次性批准。6b 因此扩展为 6b-impl-1（FFI inspect）+ 6b-impl-2（§10 诊断重构）+ 6b-apply。账本 turn 48 commit `8dc83a2`。
- [turn 50] **6b-impl-1 FFI inspect 定案**：派 sonnet implementer（bg）实现 vane-ffi vane_db_stats/vane_db_segment_info + Node N-API stats/segmentInfo + Wasm 2 函数。回报 DONE：commit `5143885`（9 文件 +801，全绑定层源码+测试，未碰 core/SPEC/计划文件）。JSON 序列化手写 serde_json::Value（复用三 crate 现有依赖零新依赖），wasm +10KB（362KB ≤800KB）。编排者核验 diff 范围 + 禁碰文件（core diff 空）+ serde_json 现有依赖确认。并行派 opus reviewer + 跑全量门禁。**opus reviewer 回报：Spec ✅，0 Critical，0 Important，3 Minor（全既有模式：FFI null 未设 thread-local/report 体积矛盾/wasm-pack test 既有），不进 fix 循环**。关键发现：Db::stats/segment_info 实为直接返回值非 Result（inspect 只读，段损坏编码 Health::Corrupt 字段），FFI 无需 VaneError→错误码映射，实现正确。7 struct 字段全覆盖。**全量门禁全绿**（fmt/clippy/test workspace/Node 21 passed/wasm32 core+vane-wasm/check-wasm-size 362636B+649619B/deny ok；no-std-fs 命中 std_fs.rs 是 VFS 实现处 SPEC 允许非违规）。review package 落档 `task-ffi-inspect-review-package.md`。**6b-impl-1 完成**（commit 5143885）。进 6b-impl-2 §10 诊断精简重构（用户 Q2 授权去 ADDITIVE/可改 enum/错误码不变/架构精简）。

## 关键纪律（每次压缩后重温）
- 编排者禁写代码/禁编辑源文件（仅 docs/plans/m4/ 计划状态文件 + 任务看板）。
- 每个实现任务：派新 implementer SubAgent → 任务后审查（spec 合规 + 质量）→ 全量本地门禁不回退 → 修复循环同一任务失败 2 次换策略/上报。
- 修复循环上限：同一 SubAgent 任务失败 2 次换策略或上报（比 SDD 默认 5 轮更保守，服从编排者）。
- 破坏性测试一律 FaultVfs/tempdir，禁真破坏宿主机。
- SPEC 修订（§9/§10/§13.2/§14）须 AskUserQuestion 用户批准。
- 每步确认可 commit。
